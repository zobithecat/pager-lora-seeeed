# 한글 자모 합성 & IME 구현 시 함정들

이 프로젝트에서 ESP32 펌웨어용 두벌식 한글 IME를 직접 작성하며
부딪힌 문제들의 정리. 데스크탑 IME에서도 거의 동일하게 적용되는
"고전적" 이슈들.

---

## 1. 한글 음절은 산술 조합이다

한글 음절 11,172자(U+AC00 ~ U+D7A3)는 다음 공식으로 결정된다.

```
syllable = 0xAC00 + (cho * 21 + jung) * 28 + jong
  cho  : 0..18  (19개 초성)
  jung : 0..20  (21개 중성)
  jong : 0..27  (28개 종성 — 0은 "종성 없음")
```

그래서 IME 상태는 `(cho, jung, jong)` 세 개의 작은 정수로 충분하다.
대신 **모든 표가 이 인덱스 체계에 일관되게 맞아야** 한다. 인덱싱이
한 곳에서 꼬이면 디버깅이 굉장히 짜증난다.

참조: [hangul_ime.cpp](hangul_ime.cpp)의 `compose_syllable()`.

---

## 2. Compatibility Jamo vs Conjoining Jamo

Unicode에는 자모를 표현하는 블록이 **두 개** 있다.

| 블록 | 범위 | 용도 |
|------|------|------|
| Hangul Compatibility Jamo | U+3131 ~ U+318E | 단독 자모 표시용 (ㄱ ㄴ ㅏ ㅑ 등) |
| Hangul Jamo (Conjoining) | U+1100 ~ U+11FF | 음절 합성용 — ligature로 음절 형태 |

함정: **두벌식 키 매핑은 보통 Compatibility 블록을 쓰지만, 내부 음절
합성은 인덱스 기반**이다. 둘을 섞으면 안 됨. 또 폰트 지원도 다르다 —
Compatibility 블록은 의외로 많은 한글 폰트에서 빠져 있다.

참조: [keymap_dubeolsik.h](keymap_dubeolsik.h)는 Compatibility 블록 사용.

---

## 3. 폰트 커버리지 — KS X 1001 vs 전체 11,172자

음절 11,172자 전부를 폰트에 넣으면 메가바이트 단위가 된다. 그래서
U8g2 같은 임베디드 폰트는 보통 **KS X 1001 완성형 2,350자**만
지원하거나, 그것도 둘로 쪼개서 (`_korean1` 약 1,000자, `_korean2`
약 2,000자) 제공한다.

함정:
- "헬"(U+D5EC), "똠"(U+B620) 같은 자주 쓰는 외래어/의성어 음절이
  `_korean1`에서 빠져 있을 수 있음 → 빈 박스(□)
- 모든 컴퍼지션 잠재 음절(11,172)을 커버하려면 자체 폰트 빌드 필요
- 그래서 **컴퍼지션 결과가 폰트에 들어 있는지** 항상 미리 확인 필요

이 프로젝트에선 `_korean2`로 바꿔서 "헬" 같이 빠진 음절을 잡았다.

---

## 4. 단독 자모는 폰트가 거의 안 가짐

조합 중 상태 — "ㄱ"만 누른 직후 — 의 미리보기는 Compatibility Jamo
하나(U+3131)다. **그런데 KS X 1001 한글 폰트들이 Compatibility Jamo
영역을 종종 빠뜨린다.** 결과: 자모 단독은 폰트가 못 그려서 화면이
빈 채로 남고, 사용자는 "내가 뭘 누르고 있는지" 알 수 없다.

가능한 해결책 셋:

### (a) Placeholder 음절 변환 (이 프로젝트의 선택)
preview()가 자음만 가진 상태라면 더미 모음 ㅡ를 끼워서 "그" 같은
음절을 그린다. commit/emit 경로에는 적용하지 않음 — 그래야
사용자가 자음 단독을 실제로 입력하면 정확한 자모가 버퍼에 들어간다.

```cpp
String preview() {
  if (cho >= 0 && jung < 0) {
    return encode_utf8(0xAC00 + (cho*21 + 18)*28);  // cho + ㅡ
  }
  // ...
  return compose_syllable(cho, jung, jong);  // 정상 케이스
}
```

장점: 코드 한 함수 수정. 단점: placeholder 음절이 "잘못된 글자"로
보임 — inverse video 박스로 "조합 중 임시" 시각 단서를 같이 줘서
완화.

### (b) 두 폰트 자동 fallback
글자별로 글리프 존재 여부(`getUTF8Width() > 0`)를 체크해서, 폰트A에
없으면 폰트B로 그린다. 한 화면 안에 두 폰트가 섞이는 게 단점.

### (c) 자모 51자 자체 비트맵 폰트
8x16 또는 16x16 셀로 51자(초성19 + 중성21 + 종성 11종 단독글리프)를
직접 그려서 `drawXBM`으로 렌더. 51 × 16 = 약 1KB. 한 번 만들면 끝.
가장 정확하지만 비트맵 노가다.

---

## 5. Preview와 Commit의 분리

이게 가장 흔한 실수. **사용자에게 보이는 미리보기**와 **실제 버퍼에
들어가는 commit 결과**는 다르게 다뤄야 한다.

망치는 케이스: 4-(a)의 placeholder 로직을 `compose_syllable()` 자체에
넣으면, commit 경로까지 placeholder가 들어간다. 결과: 사용자가 "ㄱ"만
입력하고 commit하려는데 버퍼에 "그"가 들어가는 어처구니없는 동작.

원칙: **preview는 시각용, compose_syllable은 데이터용**. 둘은 별도
함수로 두고, preview만 시각 트릭(placeholder, 박스 등)을 쓴다.

---

## 6. 복합 자모

자모는 단독뿐 아니라 두 개 합쳐서 만드는 형태도 있다.

### 복합 중성 (모음 + 모음)
| 입력 | 결과 |
|------|------|
| ㅗ + ㅏ | ㅘ |
| ㅗ + ㅐ | ㅙ |
| ㅗ + ㅣ | ㅚ |
| ㅜ + ㅓ | ㅝ |
| ㅜ + ㅔ | ㅞ |
| ㅜ + ㅣ | ㅟ |
| ㅡ + ㅣ | ㅢ |

### 복합 종성 (받침 + 받침)
| 입력 | 결과 |
|------|------|
| ㄱ + ㅅ | ㄳ |
| ㄴ + ㅈ | ㄵ |
| ㄴ + ㅎ | ㄶ |
| ㄹ + ㄱ | ㄺ |
| ㄹ + ㅁ | ㄻ |
| ㄹ + ㅂ | ㄼ |
| ㄹ + ㅅ | ㄽ |
| ㄹ + ㅌ | ㄾ |
| ㄹ + ㅍ | ㄿ |
| ㄹ + ㅎ | ㅀ |
| ㅂ + ㅅ | ㅄ |

함정:
- 두벌식 입력은 단일 자모만 받지만, **조합 결과는 복합 자모일 수 있다**
- 복합 자모 인덱스는 jong 표(28개)에 자기 슬롯이 있음 → `(cho, jung, jong)`
  안에서 깔끔하게 표현됨
- 표를 코드에 박을 때 자모 인덱스 한 번이라도 틀리면 디버깅 헬

---

## 7. 종성 → 다음 음절 초성으로 split

가장 클래식한 한글 IME 함정. **이미 종성까지 들어간 음절에 다음
모음이 오면, 종성을 떼서 다음 음절의 초성으로 옮겨야 한다.**

```
한 + ㅏ      →  하 + 나      (단일 종성 ㄴ을 통째로 이동)
갉 + ㅏ      →  갈 + 가      (복합 종성 ㄺ → ㄹ 남기고 ㄱ만 이동)
헬 + 로 ...  →  헬로 (종성 ㄹ은 이동 안 됨, ㄹ+ㄹ 불가)
```

복합 종성 split이 특히 까다롭다. 분해 표(`decompose_jong`)와
`jong → cho` 변환표 둘 다 필요.

내 코드의 처리: [hangul_ime.cpp:input_jamo](hangul_ime.cpp)에서
모음 입력이면서 `jong > 0`이면 `decompose_jong(jong)`으로 두
부분을 얻고 → 앞부분만 종성으로 남기고 → 뒷부분(또는 단일 종성
전체)을 새 음절의 초성으로.

---

## 8. Backspace 분해

직관적 동작:

```
가나다  | 백스페이스 → 가나다 (committed 글자 1개 삭제)
강      | 백스페이스 → 가 (종성만 제거)
강(=ㄱㅏㅇ) | 백스페이스 → 가 → ㄱ → 빈상태
갉(=ㄱㅏㄺ) | 백스페이스 → 갈 (복합 종성 → 단일로 분해)
과(=ㄱㅘ)   | 백스페이스 → 고 (복합 중성 → 단일로 분해)
```

원칙: **IME가 조합 중이면 한 단계만 분해, 조합 중이 아니면 버퍼에서
한 UTF-8 문자 제거**. UTF-8 분해는 continuation byte (10xxxxxx)를
거꾸로 거슬러 올라가서 grapheme 시작점까지.

---

## 9. 조합 중 시각화 (preedit visualization)

GUI IME든 임베디드든 사용자에게 "여기는 조합 중이고 곧 바뀔
글자다"를 명확히 알려야 한다. 흔한 방법:

| 방식 | 장단점 |
|------|--------|
| Underline | 가장 흔함, 미니멀. 작은 OLED에선 잘 안 보임 |
| Inverse video (배경 반전) | 강한 시각 단서. 글리프가 폰트에 없어도 위치는 보임 |
| 다른 색 | 컬러 디스플레이만 |
| 박스 외곽선 | 깔끔, 약간의 공간 낭비 |

이 프로젝트는 U8g2의 XOR draw 모드(`setDrawColor(2)`)로 해당
영역을 한 번에 inverse video로 뒤집는다. 글자가 폰트에 없어 안
그려져도 박스는 보임 → "조합 중" 단서 확실.

---

## 10. 한영 전환 키

한국 시장 키보드는 보통 한/영 키 (HID Lang1, Usage 0x90)를 갖지만,
**클론/외산 키보드는 그 키가 없다.** 대안:

| 키 | HID 코드 | 비고 |
|----|----------|------|
| 한/영 키 | 0x90 (Lang1) | 정통, 한국 키보드만 |
| 우측 Alt | 0xE6 (mod bit 6) | 한국 키보드 없을 때의 정통 fallback |
| 좌측 Alt | 0xE2 (mod bit 2) | macOS에서 ⌥로 쓰이는 경우 많음 |
| Shift+Space | 조합 | macOS Hangul IME 기본 |
| Caps Lock | 0x39 | 일부 환경 |

처리: **modifier byte의 edge detection** — 직전 보고서엔 비트가
0이고 새 보고서엔 1이면 "방금 눌렀다" → 모드 토글. 모디파이어를
계속 누르고 있으면 다시 토글되지 않음.

---

## 11. UTF-8 인코딩

한글 음절(U+AC00 ~ U+D7A3)은 **항상 3바이트 UTF-8**. 자모(U+3131
범위)도 3바이트. 옛한글이나 음절 외 영역은 다를 수 있음.

```
음절 가 (U+AC00) → 0xEA 0xB0 0x80
자모 ㄱ (U+3131) → 0xE3 0x84 0xB1
```

직접 인코딩 함수를 짜는 게 STL/외부 라이브러리보다 훨씬 작고 빠름:

```c
void encode_utf8(uint32_t cp, char* out) {
  if (cp < 0x80)        { /* 1 byte */ }
  else if (cp < 0x800)  { /* 2 bytes */ }
  else if (cp < 0x10000) {
    out[0] = 0xE0 | (cp >> 12);
    out[1] = 0x80 | ((cp >> 6) & 0x3F);
    out[2] = 0x80 | (cp & 0x3F);
  } else { /* 4 bytes */ }
}
```

---

## 12. 텍스트 폭과 줄바꿈

한글 음절은 모노스페이스에서 **ASCII의 2배 폭**으로 그려지는 게
관례 (16x16 vs 8x16). 줄바꿈 계산할 때:

- ASCII: 8px / 그리프
- 한글/CJK: 16px / 그리프
- 이걸 UTF-8 첫 바이트로 단순 판정 가능 — `byte < 0x80`이면 ASCII,
  아니면 CJK 가정 (대부분의 unifont 기반에서 정확함)

함정: `String.length()`는 **바이트** 길이. 사용자가 보는 글자 수와
다름. 한글 1글자 = 3바이트.

---

## 13. 정규화: Composed vs Decomposed

같은 한글 단어가 두 가지 방식으로 인코딩될 수 있다.

| 형태 | 표현 | 바이트 |
|------|------|--------|
| NFC (composed) | 가 = U+AC00 | 3 |
| NFD (decomposed) | ㄱ(U+1100) + ㅏ(U+1161) | 6 |

macOS Finder가 파일명을 NFD로 저장해서 한글 파일명 처리할 때
종종 함정. 본 프로젝트는 모든 음절을 NFC로 저장 (compose_syllable이
음절 단위 codepoint 생성) → 일관성 OK.

---

## 14. 키 반복 (Key Repeat)

BLE HID 키보드는 키를 누르고 있어도 **press/release 보고서를 한
번씩만** 보낸다. 사용자가 "키 길게 누르기"로 반복 입력을 기대하면
**호스트가 직접 타이머로 반복**해야 한다.

표준 동작:
- Initial delay: 250~500ms (첫 반복까지)
- Repeat rate: 30~50ms (그 후 간격)

함정: 연결 끊기면 release 보고서가 안 옴 → "키 눌린 상태"가 메모리에
남아서 반복 계속됨 → 스턱 키. **disconnect 이벤트에서 held 상태
강제 클리어** 필요.

---

## 15. Modifier와의 상호작용

자모 입력 중에 Shift, Ctrl, Cmd가 들어오면?

- **Shift**: 두벌식에선 ㄲ ㄸ ㅃ ㅆ ㅉ ㅒ ㅖ 같은 cho/jung을
  쌍자음/이중모음으로 매핑. dubeolsik_lookup에 shift 인자 필요.
- **Ctrl/Cmd**: 일반적으로 단축키. IME에 안 흘려보내고 무시.
- **Alt**: 한영 토글 (위 10번 참조).

함정: shift 조합 자음이 IME에 들어갔을 때 종성 처리 — ㄲ은 cho도
되고 jong도 되지만 ㄸ ㅃ ㅉ은 cho만 가능 (종성 자리에 못 옴).
이런 예외를 jong 표에서 0으로 표시.

---

## 16. 옛한글 / 비표준 음절

"훈민정음" 옛한글(ㆍ ㆆ ㅿ ㅸ 등)이나 KS X 1001 외 음절은 일반
IME에선 지원하지 않음. 지원하려면:

- 옛한글 자모 (U+1100 conjoining block + U+11A8~ 등)
- Hangul Jamo Extended-A, B
- 폰트가 모두 받쳐줘야 함 — 거의 없음

본 프로젝트는 옛한글 미지원.

---

## 17. 디바이스 측 vs 호스트 측 IME

BLE HID 키보드는 그냥 USB Usage code(0x04=A, 0x05=B...)를 보낸다.
**한글 변환은 호스트가 함**. 한글 키보드도 물리 layout만 한글 인쇄돼
있을 뿐, 보내는 코드는 QWERTY 위치와 동일.

함정: 두벌식 매핑이 host OS의 한국어 layout에 맞춰져 있을 수 있다.
펌웨어가 자체 IME를 돌릴 땐 host OS의 layout 설정과 무관하게
직접 USB usage → 자모 매핑 정의해야 함.

---

## 18. Thread safety (멀티 태스크 환경)

ESP32 NimBLE 콜백은 자체 태스크에서 돈다. main loop도 별도 태스크.
**둘 다 IME 상태와 텍스트 버퍼를 만지면 race condition** — 특히
Arduino `String`은 thread-safe 아님. 증상:

- 갑자기 글자가 누락
- 힙 corruption / 크래시
- 일관되지 않은 디스플레이

해결: BLE 콜백은 raw HID report를 **큐로만 전달**, IME/버퍼 mutation은
main loop 단일 태스크에서만. 본 프로젝트 [pager-lora-qwerty.ino](pager-lora-qwerty.ino)의 `s_hid_q`.

---

## 19. 한글 텍스트 길이 vs 표시 폭

```
"안녕하세요"
length()  = 15 (UTF-8 byte)
grapheme  = 5  (사람이 세는 글자 수)
display W = 80px (16px × 5)
```

세 값이 다 다름. 셋 중 어느 걸 쓸지 명확해야 함:
- 메모리 할당: byte
- 텍스트 자르기: grapheme (UTF-8 경계)
- 줄바꿈: display width

---

## 20. 한 줄 정리

한글 IME 직접 만들 때 가장 흔히 망치는 포인트들 순서대로:

1. **preview와 commit을 같은 함수로 처리** → placeholder 자동 commit되는 끔찍한 UX
2. **종성 split을 잊음** → "한아" 같이 어색한 결과
3. **복합 자모 미지원** → "ㅘ" "ㄶ" 못 만듦
4. **자모 단독이 폰트에 없는데도 그냥 그림** → 빈 화면, 사용자가 뭘 누르는지 모름
5. **Compat Jamo와 음절 영역의 폰트 커버리지 차이를 안 챙김**
6. **BLE 콜백에서 String 만짐** → 간헐적 크래시/누락
7. **키 반복을 호스트가 직접 해야 한다는 걸 안 챙김**
8. **NFC/NFD 정규화 의식 안 함** → 다른 시스템과 교환할 때 깨짐
9. **modifier edge detection 안 함** → 한/영이 계속 토글됨
10. **disconnect 시 held key 안 비움** → 스턱 키
